- [ ] Create backend dashboard summary derived from inventory + production tables
- [ ] Update frontend dashboard chart to use API data (no hardcoded run labels or loss rate)
- [ ] Validate `/api/dashboard/summary` response shape matches frontend expectations
- [ ] Run lint/build (if available) and refresh dashboard UI

- [ ] Update frontend dashboard chart to use API data (no hardcoded run labels or loss rate)
- [ ] Validate `/api/dashboard/summary` response shape matches frontend expectations
- [ ] Run lint/build (if available) and refresh dashboard UI

